plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
}

android {
    namespace "com.taxzone.owner"
    compileSdk 36

    defaultConfig {
        applicationId "com.taxzone.owner"
        minSdk 23
        targetSdk 36
        versionCode 2
        versionName "1.1.0"

        buildConfigField "String", "SUPABASE_URL", "\"https://mmpgxaptfucwcmzxdwfl.supabase.co\""
        buildConfigField "String", "SUPABASE_ANON_KEY", "\"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1tcGd4YXB0ZnVjd2Ntenhkd2ZsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODAwNTk2OTYsImV4cCI6MjA5NTYzNTY5Nn0.YL5l9YFPsS7FKb3aexRg68HK3P7ZPl7Ay6ntBFxpHek\""
    }

    buildFeatures {
        buildConfig true
    }

    signingConfigs {
        release {
            def props = new Properties()
            def propsFile = rootProject.file("release-signing.properties")
            if (propsFile.exists()) {
                propsFile.withInputStream { props.load(it) }
            }
            storeFile props["storeFile"] ? rootProject.file(props["storeFile"]) : null
            storePassword props["storePassword"]
            keyAlias props["keyAlias"]
            keyPassword props["keyPassword"]
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation platform("io.github.jan-tennert.supabase:bom:3.1.2")
    implementation "io.github.jan-tennert.supabase:gotrue-kt"
    implementation "io.github.jan-tennert.supabase:storage-kt"
    implementation "io.github.jan-tennert.supabase:realtime-kt"
    implementation "io.github.jan-tennert.supabase:postgrest-kt"
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.9.0"
    implementation "io.ktor:ktor-client-android:3.0.3"
}

